trigger: none

variables:
  - name: test 
    value: variables/vars-azure-pipeline.yml
  - template: ${{ variables.test }}


stages:
- stage: stage
  displayName: stage
  jobs:
  - job: 'job'
    pool:
      vmImage: ubuntu-latest

    steps:

    - script: echo $(var)

    - script: echo $(APP)

    - script: echo $(test)