trigger: none

variables:
  - template: variables/vars-azure-pipeline.yml
  - name: test 
    value: ${{ variables.template }}

stages:
- stage: stage
  displayName: stage
  jobs:
  - job: 'job'
    pool:
      vmImage: ubuntu-latest

    steps:

    - script: echo $(var)

    - script: echo $(APP)

    - script: echo $(test)