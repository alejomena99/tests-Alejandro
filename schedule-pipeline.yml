schedules:
- cron: '*/1 * * * *'
  displayName: Daily midnight build
  branches:
    exclude:
    - '*'

stages:
- stage: stage
  displayName: stage
  jobs:
    - job: 'job'
      pool:
        name: agent-Alejo

      steps:
      - script: echo Hello, world!
        displayName: 'Run a one-line script'
