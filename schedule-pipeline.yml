schedules:
- cron: '48 8 * * *'
  displayName: Daily 8:42 AM build
  branches:
   include:
     - '*'
  always: true

stages:
- stage: stage
  displayName: stage
  jobs:
    - job: 'job'
      pool:
        name: agent-Alejo

      steps:
      - script: echo Hello, world!
        displayName: 'Run a one-line script'
