trigger: none

variables:
  - group: alejo-var-infra

stages:
- stage: stage
  displayName: stage
  jobs:
  - job: 'job'
    pool:
      vmImage: ubuntu-latest

    steps:

    - script: echo hola Ansible

    - task: Bash@3
      inputs:
        filePath: 'scripts/az-infra.sh'
    
    - script: echo $(mySecretVal)
    - script: echo $(myVal)